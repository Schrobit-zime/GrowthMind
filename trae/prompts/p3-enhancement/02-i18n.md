# 国际化 — 添加中英文国际化支持

## 项目上下文

- 技术栈：Next.js 16 (App Router) + React 19 + TypeScript 5 + Tailwind CSS v4 + shadcn/ui
- 后端：Supabase (Auth + PostgreSQL) + Drizzle ORM
- 包管理：pnpm（严禁 npm/yarn）
- 项目路径：`/Users/jahangir/workspace/GrowthMind/projects/`
- 源码目录：`src/`
- 根布局：`src/app/layout.tsx`（当前使用 `<html lang="zh-CN" className="dark">`，包含 SupabaseConfigProvider + AuthProvider）
- 中间件：`src/middleware.ts`（当前实现角色路由守卫，matcher 为 `/((?!_next/static|_next/image|favicon.ico|.*\\.svg).*)`）
- 页面布局：`src/components/layout/app-layout.tsx`（侧边栏+顶栏，包含硬编码中文文本）
- 设计风格：毛玻璃风深色主题，背景 #070A14，主色 #7C5CFF
- 当前仅支持暗色主题
- 当前所有页面 UI 文本均为硬编码中文

## 任务目标

使用 next-intl 为项目添加中英文国际化支持，优先翻译导航栏和通用 UI 文本。

## 实施步骤

### 1. 安装 next-intl

```bash
cd /Users/jahangir/workspace/GrowthMind/projects
pnpm add next-intl
```

### 2. 创建 i18n 目录结构

```
src/i18n/
├── request.ts        # 服务端 i18n 配置（按请求获取 messages）
├── routing.ts        # 路由配置（locale prefix 策略）
│
messages/
├── zh.json           # 中文翻译
└── en.json           # 英文翻译
```

### 3. 创建翻译文件

#### `messages/zh.json` — 中文翻译

需要包含以下 key（优先翻译导航栏和通用 UI 文本）：

```json
{
  "app": {
    "title": "GrowthMind - 个人成长多维数据记录与智能分析平台",
    "description": "记录学习、工作、生活、身体、心情等多维数据，AI 驱动趋势分析与优化建议"
  },
  "nav": {
    "dashboard": "仪表盘",
    "records": "记录列表",
    "analysis": "智能分析",
    "goals": "目标管理",
    "admin": "管理员",
    "supervise": "监督面板",
    "gateway": "模型网关",
    "newRecord": "新增记录",
    "signOut": "退出登录",
    "signOutConfirm": "确认退出",
    "signOutMessage": "退出登录后需要重新登录才能访问应用",
    "cancel": "取消",
    "confirm": "确认退出"
  },
  "common": {
    "loading": "加载中...",
    "noData": "暂无数据",
    "search": "搜索...",
    "save": "保存",
    "delete": "删除",
    "edit": "编辑",
    "back": "返回",
    "all": "全部"
  },
  "dashboard": {
    "title": "下午好，{name}",
    "todayOverview": "今日概览",
    "moodTrend": "心情趋势",
    "expandMore": "展开更多",
    "collapse": "收起图表",
    "fiveDimension": "五维综合评估",
    "monthlyActivity": "本月活跃度",
    "goalProgress": "目标进度",
    "week": "周",
    "month": "月"
  },
  "records": {
    "title": "记录列表",
    "description": "浏览和管理你的所有成长记录",
    "noRecords": "暂无记录",
    "searchRecords": "搜索记录..."
  },
  "timeDimensions": {
    "daily": "日报",
    "weekly": "周报",
    "monthly": "月报",
    "annual": "年报",
    "morning": "早报",
    "noon": "午报",
    "evening": "晚报",
    "custom": "自定义"
  },
  "dimensions": {
    "learning": "学习",
    "work": "工作",
    "life": "生活",
    "health": "身体",
    "mood": "心情"
  },
  "auth": {
    "login": "登录",
    "register": "注册",
    "email": "邮箱",
    "password": "密码",
    "displayName": "显示名称",
    "unauthorized": "未授权",
    "permissionDenied": "权限不足"
  },
  "user": {
    "role": "普通用户",
    "roleAdmin": "管理员"
  }
}
```

#### `messages/en.json` — 英文翻译

```json
{
  "app": {
    "title": "GrowthMind - Personal Growth Multi-Dimensional Data & AI Analysis Platform",
    "description": "Track learning, work, life, health, and mood. AI-driven trend analysis and optimization suggestions"
  },
  "nav": {
    "dashboard": "Dashboard",
    "records": "Records",
    "analysis": "Analysis",
    "goals": "Goals",
    "admin": "Admin",
    "supervise": "Supervision",
    "gateway": "Gateway",
    "newRecord": "New Record",
    "signOut": "Sign Out",
    "signOutConfirm": "Confirm Sign Out",
    "signOutMessage": "You will need to log in again to access the app",
    "cancel": "Cancel",
    "confirm": "Confirm"
  },
  "common": {
    "loading": "Loading...",
    "noData": "No data",
    "search": "Search...",
    "save": "Save",
    "delete": "Delete",
    "edit": "Edit",
    "back": "Back",
    "all": "All"
  },
  "dashboard": {
    "title": "Good afternoon, {name}",
    "todayOverview": "Today's Overview",
    "moodTrend": "Mood Trend",
    "expandMore": "Show More",
    "collapse": "Collapse",
    "fiveDimension": "5-Dimension Assessment",
    "monthlyActivity": "Monthly Activity",
    "goalProgress": "Goal Progress",
    "week": "Week",
    "month": "Month"
  },
  "records": {
    "title": "Records",
    "description": "Browse and manage all your growth records",
    "noRecords": "No records yet",
    "searchRecords": "Search records..."
  },
  "timeDimensions": {
    "daily": "Daily",
    "weekly": "Weekly",
    "monthly": "Monthly",
    "annual": "Annual",
    "morning": "Morning",
    "noon": "Noon",
    "evening": "Evening",
    "custom": "Custom"
  },
  "dimensions": {
    "learning": "Learning",
    "work": "Work",
    "life": "Life",
    "health": "Health",
    "mood": "Mood"
  },
  "auth": {
    "login": "Login",
    "register": "Register",
    "email": "Email",
    "password": "Password",
    "displayName": "Display Name",
    "unauthorized": "Unauthorized",
    "permissionDenied": "Permission Denied"
  },
  "user": {
    "role": "User",
    "roleAdmin": "Admin"
  }
}
```

### 4. 创建 `src/i18n/request.ts`

```typescript
// src/i18n/request.ts
import { getRequestConfig } from "next-intl/server";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  // 从请求中获取 locale，fallback 到默认语言
  let locale = await requestLocale;

  if (!locale || !routing.locales.includes(locale as "zh" | "en")) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

### 5. 创建 `src/i18n/routing.ts`

```typescript
// src/i18n/routing.ts
import { defineRouting } from "next-intl/routing";

export const routing = defineRouting({
  // 支持的语言列表
  locales: ["zh", "en"],
  // 默认语言
  defaultLocale: "zh",
  // 语言前缀策略：as-needed（默认语言不带前缀，其他语言带前缀）
  localePrefix: "as-needed",
  // 域名策略（单域名，不使用不同域名）
  domains: [],
});
```

### 6. 修改 `src/app/layout.tsx`

需要先读取当前 `src/app/layout.tsx`（当前内容见项目上下文），因为 `next-intl` 要求 `params.locale` 可用，需要将 layout 改为接收 `params`。

修改前（当前代码）：
```tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="zh-CN" className="dark">
      <body>...</body>
    </html>
  );
}
```

修改后（集成 next-intl）：
```tsx
import type { Metadata } from "next";
import "./globals.css";
import { SupabaseConfigProvider } from "@/components/auth/supabase-config-provider";
import { AuthProvider } from "@/components/auth/auth-provider";
import { NextIntlClientProvider } from "next-intl";
import { getMessages } from "next-intl/server";
import { routing } from "@/i18n/routing";

export const metadata: Metadata = {
  title: "GrowthMind - 个人成长多维数据记录与智能分析平台",
  description:
    "记录学习、工作、生活、身体、心情等多维数据，AI 驱动趋势分析与优化建议",
};

export default async function RootLayout({
  children,
  params,
}: Readonly<{
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}>) {
  const { locale } = await params;
  const messages = await getMessages();

  return (
    <html lang={locale === "en" ? "en" : "zh-CN"} className="dark">
      <body className="min-h-screen bg-background text-foreground antialiased">
        <NextIntlClientProvider messages={messages}>
          <SupabaseConfigProvider>
            <AuthProvider>{children}</AuthProvider>
          </SupabaseConfigProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

### 7. 创建 `src/middleware.ts`（替换现有中间件）

需要先读取当前 `src/middleware.ts`（当前内容见项目上下文），然后将 next-intl 的 middleware 与现有角色路由守卫合并。

修改后的 middleware：
```typescript
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";
import { createClient } from "@supabase/supabase-js";
import { routing } from "@/i18n/routing";
import createMiddleware from "next-intl/middleware";

const adminRoutes = ["/supervise", "/gateway"];

function isAdminRoute(pathname: string): boolean {
  return adminRoutes.some((r) => pathname.includes(r));
}

// next-intl 中间件
const intlMiddleware = createMiddleware(routing);

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  // Skip static assets and API config
  if (
    pathname.startsWith("/_next") ||
    pathname.startsWith("/favicon") ||
    pathname.match(/\.(svg|ico|png|jpg|jpeg|gif|webp)$/) ||
    pathname === "/api/supabase-config"
  ) {
    return NextResponse.next();
  }

  // Get Supabase session from cookies
  const supabaseUrl = process.env.COZE_SUPABASE_URL;
  const supabaseAnonKey = process.env.COZE_SUPABASE_ANON_KEY;

  if (!supabaseUrl || !supabaseAnonKey) {
    return intlMiddleware(request);
  }

  const accessToken =
    request.cookies.get("sb-access-token")?.value ||
    request.cookies.get("sb-growthmind-auth-token")?.value;

  const publicRoutes = ["/login", "/api/supabase-config"];
  const isPublic = publicRoutes.some((r) => pathname.includes(r));

  if (!accessToken && !isPublic) {
    if (pathname.startsWith("/api/")) {
      return NextResponse.json(
        { success: false, error: "未授权" },
        { status: 401 }
      );
    }
    return NextResponse.redirect(new URL("/login", request.url));
  }

  // Admin route protection
  if (isAdminRoute(pathname) && accessToken) {
    try {
      const supabase = createClient(supabaseUrl, supabaseAnonKey, {
        global: { headers: { Authorization: `Bearer ${accessToken}` } },
        auth: { autoRefreshToken: false, persistSession: false },
      });

      const { data: { user } } = await supabase.auth.getUser(accessToken);

      if (user) {
        const { data: profile } = await supabase
          .from("profiles")
          .select("role")
          .eq("user_id", user.id)
          .maybeSingle();

        if (profile?.role !== "admin") {
          if (pathname.startsWith("/api/")) {
            return NextResponse.json(
              { success: false, error: "权限不足" },
              { status: 403 }
            );
          }
          return NextResponse.redirect(new URL("/", request.url));
        }
      }
    } catch {
      if (!pathname.startsWith("/api/")) {
        return NextResponse.redirect(new URL("/login", request.url));
      }
    }
  }

  return intlMiddleware(request);
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico|.*\\.svg|api).*)"],
};
```

### 8. 修改 `src/components/layout/app-layout.tsx`

需要先读取当前文件，将硬编码中文替换为 `useTranslations` hook：

```typescript
// 在组件顶部添加
import { useTranslations } from "next-intl";

// 在组件内部
const t = useTranslations("nav");

// 替换 navItems 中的 label
const navItems = [
  { href: "/", label: t("dashboard"), icon: LayoutDashboard },
  { href: "/records", label: t("records"), icon: FileText },
  { href: "/analysis", label: t("analysis"), icon: Brain },
  { href: "/goals", label: t("goals"), icon: Target },
];

// 替换硬编码文本
// "管理员" -> t("admin")
// "新增记录" -> t("newRecord")
// "退出登录" -> t("signOut")
// "确认退出" -> t("signOutConfirm")
// 等等
```

### 9. 修改 `src/app/(main)/page.tsx`（仪表盘）

需要先读取当前文件，将硬编码中文替换为 `useTranslations`：

```typescript
import { useTranslations } from "next-intl";

// 在组件内部
const t = useTranslations("dashboard");
const tCommon = useTranslations("common");
const tDim = useTranslations("dimensions");

// 替换文本
// "下午好，{displayName}" -> t("title", { name: displayName })
// "心情趋势" -> t("moodTrend")
// "展开更多" -> t("expandMore")
// "收起图表" -> t("collapse")
// "五维综合评估" -> t("fiveDimension")
// "本月活跃度" -> t("monthlyActivity")
// "目标进度" -> t("goalProgress")
// "周"/"月" -> t("week") / t("month")
```

### 10. 修改 `src/app/(main)/records/page.tsx`（记录列表）

需要先读取当前文件，将硬编码中文替换为 `useTranslations`。

### 11. 验证 i18n 路由配置

```bash
pnpm install
pnpm build
pnpm ts-check
```

确保 URL 路由正确：
- 中文（默认）：`/` → 中文页面
- 英文：`/en` → 英文页面
- 手动切换：`/en/records` → 英文记录页

## 验收标准

- [ ] `next-intl` 正确安装，无版本冲突
- [ ] `messages/zh.json` 和 `messages/en.json` 翻译文件完整
- [ ] `src/i18n/request.ts` 和 `src/i18n/routing.ts` 配置正确
- [ ] `src/app/layout.tsx` 正确集成 `NextIntlClientProvider`
- [ ] `src/middleware.ts` 同时支持 i18n 路由和角色路由守卫
- [ ] `src/components/layout/app-layout.tsx` 导航栏文本已国际化
- [ ] 仪表盘和记录列表页面关键文本已国际化
- [ ] 访问 `/en` 路径显示英文界面
- [ ] 默认路径 `/` 显示中文界面
- [ ] `pnpm build` 构建成功
- [ ] `pnpm ts-check` 类型检查通过

## 预估工时

2 人天

## 依赖

无 — 全部可并行